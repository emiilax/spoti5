<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spoti5.ecobussing" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />


    <application

        android:name=".EcoTravelApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo_compact"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait"
        >

        <activity
            android:name=".Activites.MainActivity"
            android:windowSoftInputMode="stateUnchanged|adjustPan"
            android:label="@string/title_activity_drawer">
        </activity>

        <activity
            android:name=".Activites.StartActivites"
            android:label="@string/title_start_activities">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity android:name=".Activites.OverviewActivity"></activity>
        <activity android:name=".Activites.RegisterActivity"></activity>
        <activity android:name=".Activites.LoginActivity" ></activity>
    </application>
</manifest>
